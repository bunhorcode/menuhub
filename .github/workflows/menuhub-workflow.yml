name: menuhub-workflow

#To get a trigger on every push on this repository in any branch
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run a command
        run: echo "Hello, world"
